apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-content
        configMap:
          name: app3-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app3-html
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <title>App3</title>
        <style>
            body { 
                background: linear-gradient(135deg, #8360c3 0%, #2ebf91 100%);
                color: white; 
                font-family: Arial, sans-serif; 
                text-align: center; 
                padding-top: 100px; 
            }
            h1 { font-size: 3em; margin-bottom: 20px; }
            p { font-size: 1.5em; }
        </style>
    </head>
    <body>
        <h1>Application 3</h1>
        <p>Welcome to App3 - Nginx Server</p>
        <p>Host: app3.com</p>
    </body>
    </html>
---
apiVersion: v1
kind: Service
metadata:
  name: app3-service
spec:
  selector:
    app: app3
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80